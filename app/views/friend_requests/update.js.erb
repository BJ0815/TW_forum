// remove好友邀請並在 #friend 顯示好友
$('#incoming-<%= @friend_request.id %>').remove();
$('#friends').html("<%= j( render 'shared/friend', user: current_user ) %>");
$(function(){
  toastr.success("成功加為好友！");
});