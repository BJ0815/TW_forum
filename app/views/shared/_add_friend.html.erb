<%= link_to "add friend", friend_requests_path(friend_id: @post.user), method: :post, class: "btn btn-success" unless current_user == @post.user || 
                              current_user.friends.include?(@post.user) ||current_user.friend_requests.include?(@post.user) || 
                                @post.user.friend_requests.include?(current_user) %>